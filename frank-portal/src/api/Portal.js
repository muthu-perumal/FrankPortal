// Minimal API wrapper used by ApplicationFormPage.
// Replace the implementation with your real backend call.

export async function getInboxItem(workflowId, processId, transactionId) {
  void workflowId
  void processId
  void transactionId

  // Safe default shape.
  return { formData: { fields: {} } }
}

